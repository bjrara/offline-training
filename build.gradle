allprojects {
  repositories {
    mavenCentral()
  }
}

subprojects {
  apply plugin: 'java'
  apply plugin: 'eclipse'
  group = "com.offline.training"
}

project(':homework') {
  dependencies {
    testCompile 'junit:junit:4.11'
  }
}
